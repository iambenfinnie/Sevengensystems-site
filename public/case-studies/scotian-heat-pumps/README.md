# Scotian Heat Pumps Case Study — Image Drop Folder

Drop screenshots here. The page references:

- `leaderboard-hero.png` — anonymized full-leaderboard view (YTD or current month). Replace rep names with "Rep A / Rep B / Rep C" before saving. Recommended export: 2400×1500, PNG, dark background preserved.

Optional supporting images you can add later (will need a small page edit to surface):

- `per-rep-detail.png` — one rep's monthly history view, anonymized.
- `weekly-vs-monthly.png` — split view showing the time-slicing capability.
- `tablet-mounted.png` — physical photo of the bullpen-mounted tablet, if shareable.

Until images land, the hero will show a broken image. That's intentional — it's the visible reminder that this asset is still pending.
